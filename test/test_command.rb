require 'helper'

class TestCommand < Test::Unit::TestCase

  context "protected methods" do

    # should "read links file" do
    #   assert !Scourb::Command.read_links.empty?
    # end

    # should "read words file" do
    #   assert !Scourb::Command.read_words.empty?
    # end

    # should "read first link" do
    #   file = File.join('dict', 'links.txt')
    #   first = IO.readlines(file)[0]
    #   assert_equal first, Scourb::Command.read_links[0]
    # end

    # should "read first word" do
    #   file = File.join('dict', 'words.txt')
    #   first = IO.readlines(file)[0]
    #   assert_equal first, Scourb::Command.read_words[0]
    # end
    
  end


end
